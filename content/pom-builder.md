---
created: 2026-05-08
tags:
  - testing
  - AI
---
# Definition:
Là một skill giúp chuyển hóa các Gherkin features đã được QA xác nhận thành [[Locator Factory]] và [[POM (Page Object Model)]] thông qua [[playwright-cli.]]

# Inputs
1. **Feature files reviewed by QA**
2. **Live URL** (preferred)OR a **saved DOM snapshot**: 
3. **`playwright-cli` installed**
4. **Permission allowlist**: `.claude/settings.json` must contain `Bash(playwright-cli *)` under `permissions.allow`.

# Outputs

# Workflow

# Problem
Dễ xảy ra vấn đề ko tìm đúng locator hoặc mất quá nhiều thời gian để tìm được locator đúng cách.
